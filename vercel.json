{
  "buildCommand": "test -d frontend && (cd frontend && npm install && npm run build) || (npm install && npm run build)",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
